# StackBlitzで最初にすること

1. ZIPを展開してGitHubリポジトリへ入れる
2. StackBlitzで **Import from GitHub**
3. Terminalで `npm install`
4. `npm run dev`
5. Previewで `/`, `/characters/`, `/characters/01/`, `/works/`, `/stance/` を確認

Next.jsなので `index.html` を直接編集する方式ではありません。ページは `src/app/`、データは `src/data/`、共通部品は `src/components/` を編集します。
