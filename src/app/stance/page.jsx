import PageTitle from "@/components/PageTitle";
import StanceAccordion from "@/components/StanceAccordion";
import { stance } from "@/data/stance";
import { site, absoluteUrl } from "@/data/site";
export const metadata = { title: "STANCE", description: "Profile / interaction policy.", alternates: { canonical: `${site.siteUrl}stance/` }, openGraph: { title: `STANCE | ${site.siteName}`, description: "Profile / interaction policy.", url: `${site.siteUrl}stance/`, images: [{ url: absoluteUrl(site.defaultOgp), width: 1200, height: 630 }] }, twitter: { card: "summary_large_image", title: `STANCE | ${site.siteName}`, description: "Profile / interaction policy.", images: [absoluteUrl(site.defaultOgp)] } };
export default function StancePage() { return <section className="page subpage stance-page"><div className="section"><div className="wrap"><PageTitle>STANCE</PageTitle><div className="stance-shell"><section className="stance-intro"><div className="stance-eyebrow">{stance.intro.eyebrow}</div><h2 className="stance-intro-title">{stance.intro.title}</h2><div className="stance-tags">{stance.intro.tags.map((tag) => <span className="stance-tag" key={tag}>{tag}</span>)}</div><p className="stance-lead">{stance.intro.text}</p></section><StanceAccordion sections={stance.sections} /><section className="contact stance-contact"><div className="stance-contact-kicker">CONTACT / LINKS</div><div className="contact-big">LET&apos;S<br />TALK.</div><div className="contact-links">{site.contact.map((item) => <a className="contact-link" href={item.url} key={item.label} target={item.url.startsWith('http') ? '_blank' : undefined} rel="noreferrer"><span>{item.label}</span><span aria-hidden="true">↗</span></a>)}</div></section></div></div></div></section>; }
